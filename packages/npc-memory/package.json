{
  "name": "@aetherlife/npc-memory",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate"
  },
  "dependencies": {
    "@aetherlife/shared": "workspace:*",
    "drizzle-orm": "^0.38.3",
    "postgres": "^3.4.7"
  },
  "devDependencies": {
    "drizzle-kit": "^0.30.1",
    "vitest": "^2.1.8"
  }
}
