# Optional: run ai-gateway in a container. Postgres/Redis use Supabase + Upstash (.env).
services:
  ai-gateway:
    build:
      context: ..
      dockerfile: apps/ai-gateway/Dockerfile
    ports:
      - "127.0.0.1:8000:8000"
