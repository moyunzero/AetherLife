/**
 * Wave 0 stub — Phase 28 useNpcRelationships hook (D-API-01, D-GRAPH-04).
 * GREEN fill: plan 10 (GET on tab open + relationshipSync invalidate refetch).
 */
import { describe, it } from "vitest";

describe("useNpcRelationships (Wave 0)", () => {
  it.skip("D-GRAPH-04: fetches band-mapped edges on tab open via GET npc-relationships", () => {
    // TODO plan 10: hook loads when Drawer Tab「关系网」opens
  });

  it.skip("D-API-01 / D-GRAPH-04: relationshipSync invalidate triggers refetch without body in message", () => {
    // TODO plan 10: Colyseus hint → HTTP GET again
  });
});
