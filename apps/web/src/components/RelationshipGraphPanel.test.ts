/**
 * Wave 0 stub — Phase 28 RelationshipGraphPanel (D-GRAPH-01…03, D-VERIFY-01).
 * GREEN fill: plan 10. Band chips only — no raw affection/trust integers in DOM.
 */
import { describe, it } from "vitest";

describe("RelationshipGraphPanel (Wave 0)", () => {
  it.skip("D-GRAPH-01: renders Drawer Tab surface「关系网」with data-testid relationship-graph-panel", () => {
    // TODO plan 10: top-level drawer tab, not roster-embedded
  });

  it.skip("D-GRAPH-02: shows band chips / kind labels — DOM must not contain raw affection/trust integers", () => {
    // TODO plan 10: assert no bare score integers in markup
  });

  it.skip("D-GRAPH-03: default ego-centered star from selected NPC with toggle to full 12-node graph", () => {
    // TODO plan 10: ego vs full toggle
  });
});
